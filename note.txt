{{localhost}}/api/brazing/update

{
    "zone2": 500
}

**WORKFLOW**
- Camera have 1 upto 4 zone
- Each zone have number digit likes 0-999 maybe but our model detect 0-9 we need to format it to 0-999 (count from left to right)
- 1 Machine have 4 camera (at least for now) that we have 12 zones
- Group data from all cameras (all zones) to 1 payload
{
    "zone1" : 500,
    "zone2" : 200
}

Ex.
1. {"zone1": 500, "zone2": 200} --> send failed
2. cache it {"zone1": 500, "zone2": 200} new value update {"zone1": 500, "zone2": 200, "zone3": 540} --> try to send --> send failed
3. cache it {"zone1": 500, "zone2": 200, "zone3": 540} new value update {"zone1":600} --> {"zone1": 600, "zone2": 200, "zone3": 540} --> try to send --> 200 success